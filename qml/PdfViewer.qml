import QtQuick
import QtQuick.Controls

ApplicationWindow {
    id: root
    width: 560
    height: 640
    title: "Visor Presupuesto"

    Label {
        text: "new window"
        anchors.centerIn: parent
    }
}
